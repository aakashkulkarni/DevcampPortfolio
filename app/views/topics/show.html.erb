<div class="col-sm-8 blog-main">

  <h1><%= @topic.title %></h1>
  
  <% if logged_in?(:site_admin) %>
    <%= link_to fa_icon('pencil-square-o'), edit_topic_path %>
    <%= link_to fa_icon('trash'), @topic, method: :delete, data: { confirm: 'Are you sure you want to delte this topic?' } %>
  <% end %>
  
  <hr>
  
  <%= paginate @blogs %>
  
  <%= render @blogs %>
  
  <%= paginate @blogs %>

</div>
</head>
<body>


